[package]
name = "cpu_verifier"
version = "0.1.0"
edition = "2021"

[dependencies]
# No production dependencies needed for the tests library itself

[dev-dependencies]
marlin = "0.10"
rand = "0.8"
bitflags = "2.4"

[build-dependencies]
marlin = "0.10"
