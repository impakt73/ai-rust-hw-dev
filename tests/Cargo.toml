[package]
name = "cpu_verifier"
version = "0.1.0"
edition = "2021"

[dependencies]
# No production dependencies needed for the tests library itself

[dev-dependencies]
marlin = { version = "0.10", features = ["verilog"] }
rand = "0.8"
bitflags = "2.4"
